<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomePage | MyCommerce</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>
<body class="w-screen min-h-screen overflow-x-hidden">
    <nav class="w-full p-3 sm:px-5 shadow-md flex items-center bg-slate-50">
        <h1 class="text-xl lg:text-2xl font-semibold">
            <a href="#">MyCommerce</a>
            <i class="ri-shopping-cart-fill text-xl lg:text-2xl"></i>
        </h1>
        <i class="ri-flashlight-fill ml-auto text-2xl lg:text-3xl text-yellow-400 rotate-12"></i>
    </nav>
    <main class="w-full p-4">
        <h1 class="text-3xl lg:text-4xl font-semibold italic">Welcome <%= name %></h1>
        <hr class="my-3 h-[2px] w-full bg-black">
        <main class="w-full py-4 flex flex-col md:flex-row gap-8">
            <div id="sidebar" class="flex flex-col gap-4 w-max p-4">
                <p>
                    <a class="font-semibold text-lg text-gray-500" href="/owner/all">All Products</a>
                </p>
                <p>
                    <a class="font-semibold text-lg" href="#">Create New Product</a>
                </p>
            </div>
            <div id="mainbar" class="flex-1 p-4 space-y-6">
                <h1 class="font-semibold text-2xl lg:text-3xl">Shop For New Product</h1>
                
                <div class="flex gap-4 flex-wrap">
                    <% allProducts.forEach(product => { %>
                        <div class="shadow-md w-max rounded-lg overflow-hidden">
                            <div class="bg-[#<%= product.panelColor %>] p-4 text-[#<%= product.textColor %>]">
                                <h1 class="font-semibold text-xl lg:text-xl capitalize"><%= product.name %></h1>
                                <p class="text-lg font-semibold">$<%= Math.floor(product.price * (1 - product.discount/100)) %> 
                                    <span class="line-through">$<%= product.price %></span> 
                                    <span class="text-red-600">(<%= product.discount %>% off)</span>
                                </p>
                                <!-- <button class="bg-blue-500 text-white px-4 py-2 rounded-lg">Add to Cart</button> -->
                            </div>
                            <div class="p-4 bg-[#<%= product.bgColor %>]">
                                <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="<%= product.name %> Image" class="mt-4 w-40 h-40 object-contain mx-auto">
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </main>
    </main>
</body>
</html>
